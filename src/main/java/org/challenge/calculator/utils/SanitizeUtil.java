package org.challenge.calculator.utils;

/**
 * The intention of this utility class is to detect and remove
 * possible dangerous input received through the controllers.
 * Right now it just returns the same string, so it's more like a placeholder
 * that further work.
 */
public class SanitizeUtil {
    static public String sanitize(String input){
        return input;
    }
}
